# Bug Report Template — IEEE 1044 (Software Anomalies Classification)

Use this template to generate the specification document for `fix/` work items. This replaces the SRS for bug fixes, focusing on anomaly documentation rather than requirements elicitation.

> **OKF profile (mandatory).** Every generated document begins with the YAML frontmatter below. It makes the `.specs/` bundle a portable, machine-readable knowledge graph and is what the pipeline gates read (via `status`). See the profile contract in `doc/design/okf-perfil-sddk.md`. Fill the frontmatter FIRST, then the body. The document body follows IEEE 1044 unchanged.

---

## Document Structure

```markdown
---
type: bug-report
title: "Bug Report — {Bug Name}"
description: "{one-line summary of the anomaly}"
status: draft            # draft → approved (only 'approved' unlocks the SDD stage)
work_item: "{module-acronym}-{number}-{kebab-description}"
work_type: fix
tags: [{module-acronym}]
timestamp: {ISO 8601, e.g. 2026-08-05T14:30:00Z}
traces:
  - rel: governed-by
    target: ../../standards/coding-standards.md
---

# Bug Report
## {Bug Name}

**Version**: 1.0
**Date**: {creation date}
**Project**: {project name}
**Bug**: {bug name}
**Severity**: Critical | High | Medium | Low

---

## 1. Summary

One-sentence description of the anomaly.

---

## 2. Environment

| Attribute | Value |
|:---|:---|
| Environment | Production / Staging / Local |
| Browser/Platform | {details} |
| Version/Commit | {version or commit hash} |
| User role | {role when bug occurs} |
| Date discovered | {date} |

---

## 3. Behavior

### 3.1 Expected Behavior
What SHOULD happen according to the original specification or intended design.
Reference the original SRS requirement if applicable (e.g.: `FR-001 in features/{feature}/srs.md`).

### 3.2 Actual Behavior
What ACTUALLY happens. Include exact error messages, incorrect outputs, or unexpected states.

### 3.3 Steps to Reproduce
1. {step 1}
2. {step 2}
3. {step 3}
4. Observe: {what goes wrong}

### 3.4 Frequency
- [ ] Always reproducible
- [ ] Intermittent (approximate rate: {X}% of attempts)
- [ ] Only under specific conditions: {describe conditions}

---

## 4. Impact Analysis

### 4.1 Affected Features

| Feature | Impact | SRS Reference |
|:---|:---|:---|
| {feature name} | {how it's affected} | {FR-xxx if applicable} |

### 4.2 Affected Users
Which users/personas are impacted and how severely.

### 4.3 Data Impact
Is any data being corrupted, lost, or exposed incorrectly?

### 4.4 Workaround
Is there a temporary workaround available? Describe it.

---

## 5. Evidence

### 5.1 Logs
```
{paste relevant log output}
```

### 5.2 Screenshots / Recordings
{embed or link to visual evidence}

### 5.3 Network Traces
{relevant request/response data if applicable}

---

## 6. Acceptance Criteria

### AC-001: {Criterion Name}
- **Given**: {pre-condition}
- **When**: {action that previously triggered the bug}
- **Then**: {expected correct result after fix}

### AC-002: {Criterion Name}
(repeat for each criterion)

---

## 7. Constraints

Technical or business constraints that affect the fix approach.

---

## 8. Traceability

| Bug Report Section | Related Feature | Related SRS | Related SDD |
|:---|:---|:---|:---|
| 3. Behavior | {feature name} | {FR-xxx} | {SDD section} |

---

## 9. Root Cause Investigation

> This section is generated during Phase 1B by the agent. It documents the scientific debugging process used to identify the root cause.

### 9.1 Files Investigated

| File | Relevance | Discovery Method |
|:---|:---|:---|
| {path} | High / Medium / Low | SDD reference / grep search / user indicated |

### 9.2 Hypotheses Evaluated

| # | Hypothesis | Probability | Status | Evidence |
|:---|:---|:---|:---|:---|
| H1 | {description} | 🟢 High | ✅ Confirmed / ❌ Eliminated | {code evidence or user response} |
| H2 | {description} | 🟡 Medium | ❌ Eliminated | {why it was eliminated} |
| H3 | {description} | 🟠 Low | ❌ Eliminated | {why it was eliminated} |

### 9.3 Root Cause Confirmed

- **File**: {path}
- **Function**: {function name}
- **Line**: {approximate line number}
- **Description**: {what is wrong and why it causes the observed behavior}
- **Why it wasn't caught**: {e.g.: no test coverage, edge case not considered in SRS}
```

## Filling Rules

1. **Steps to reproduce MUST be specific and numbered** — a developer must be able to reproduce the bug from scratch
2. **Expected vs actual behavior MUST reference the original specification** when available
3. **Each acceptance criterion MUST be verifiable** — Given/When/Then format preferred
4. **Severity MUST be classified** using the same scale as the refactoring-severity-guide (Critical/High/Medium/Low)
5. **Evidence MUST include at least one type** — logs, screenshots, or network traces
6. **Impact analysis MUST cover data impact** — is any data being corrupted or lost?
7. **Section 9 is generated by the agent during Phase 1B** — the agent reads code, forms hypotheses, and asks discriminating questions to eliminate them until the root cause is confirmed
8. **OKF frontmatter is mandatory** — always emit it, with `type: bug-report` and `status: draft` on creation. Set `status: approved` only after the user approves the document. `work_item` MUST match the fix folder name exactly

